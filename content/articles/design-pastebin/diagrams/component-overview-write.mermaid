%% Diagram: Component overview — write path
%% Type: flowchart
%%{init: {'htmlLabels': false}}%%
flowchart TB
    CLIENTS["Clients<br/>browser · CLI · SDK"]
    CDN["CDN"]
    WAF["WAF"]
    LB["Load balancer"]

    subgraph API["API gateway"]
        RATE["Rate limiter"]
        AUTH["Auth"]
        ROUTER["Router"]
    end

    PASTE["Paste Write Service"]
    KGS["Key Generation Service"]
    SCAN["Content scanner"]

    STORAGE["Storage tier<br/>Redis · PostgreSQL · S3"]
    QUEUE["Job queue<br/>SQS / Kafka"]
    WORKERS["Highlight workers"]

    CLIENTS --> CDN -->|miss| WAF --> LB
    LB --> RATE --> AUTH --> ROUTER --> PASTE
    PASTE --> KGS
    PASTE --> SCAN
    PASTE --> STORAGE
    PASTE --> QUEUE --> WORKERS
