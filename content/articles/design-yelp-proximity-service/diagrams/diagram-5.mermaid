%% Diagram: Diagram 5
%% Type: sequence
%%{init: {'htmlLabels': false}}%%
sequenceDiagram
    participant U as User
    participant API as API
    participant M as Moderation

    U->>API: POST /reviews
    API->>API: insert (pending)
    API->>M: ReviewCreated (via Kafka)
    API-->>U: 201 Created
    M->>M: spam + toxicity
    M->>API: approved or manual_review
